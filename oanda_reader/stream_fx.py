from oanda_reader import OandaReader

if __name__ == "__main__":
    # get some stuff
    oanda_reader = OandaReader()

    # spawn streaming
    # fx_stream = OandaStreamer(access_token=t)
    # fx_stream.rates(account_id=a, instruments="EUR_USD")
    #
    # oanda = oandapy.API(environment="practice", access_token=t)
    #
    # response = oanda.get_prices(instruments="EUR_USD")
    # prices = response.get("prices")
    # asking_price = prices[0].get("ask")
    # print prices



